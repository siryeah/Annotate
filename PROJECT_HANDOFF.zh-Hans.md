# Annotate 中文定制版：产品决策与开发交接文档

> 最后更新：2026-08-03
>
> 适用对象：项目维护者、后续接手开发的 AI 或工程师
>
> 文档原则：区分“最初想法”“已经实现”“尚未完成”，不要把讨论方案误写成现有能力。

## 1. 项目速览

| 项目 | 当前信息 |
| --- | --- |
| 产品名称 | Annotate（本地应用名称暂不改） |
| 定制版仓库 | <https://github.com/siryeah/annotate-cn-custom> |
| 原始项目 | <https://github.com/epilande/Annotate> |
| 原作者 | Emmanuel Pilande（`epilande`） |
| 定制版维护者 | AI 产品经理四月（GitHub：`@siryeah`） |
| 当前发布版本 | `v1.4.1-cn.1` |
| 发布基线提交 | `4a2ae01` |
| 系统要求 | macOS 14+、Apple Silicon |
| Bundle ID | `com.siryeah.Annotate` |
| 开源许可 | MIT；必须保留原作者版权和许可声明 |

这是基于原版 Annotate 的非官方简体中文定制版。主要使用场景是直播授课、网页讲解、软件实操和屏幕共享，而不是做成通用图片编辑器。

## 2. 最初为什么要做

维护者是一名 AI 产品经理和讲师，经常在线直播授课。PPT 放映时可以用内置画笔，但一旦切换到浏览器、AI 工具或其他应用进行实操，就没有方便的屏幕标注能力。

最初需求是：

1. 通过简单快捷键进入画笔状态，直接在当前演示屏幕上圈重点、划线或画箭头。
2. 标注颜色可以配置，默认可使用红色，也可以选择橙色、黄色等。
3. 双屏时不应同时遮住所有屏幕，只在触发快捷键时使用鼠标所在屏幕；单屏也要自然工作。
4. 快捷键尽量是两个键，最多三个键，并且按键位置要近，方便直播过程中单手操作。
5. 尽量避免与浏览器、PPT 或直播软件快捷键冲突，尤其不能让退出画笔的操作意外退出浏览器全屏。
6. 标注应尽可能被录屏或直播软件录到。

最初曾考虑只做手绘线、直线、箭头和圆角矩形的极简 MVP。后来发现开源项目 Annotate 已具备比较完整的基础能力，因此最终策略改为：保留原版的大部分工具，只针对实际授课体验做中文化、圆角矩形、画笔光标、淡出时长、快捷键分层和性能优化。

## 3. 已确定的产品原则

### 3.1 多屏策略

- 程序会为每个 `NSScreen` 准备覆盖窗口，但按 `⌥Z` 时只激活鼠标当前所在屏幕的绘图层。
- 鼠标所在屏幕通过 `NSEvent.mouseLocation` 和屏幕 frame 判断。
- 显示器接入或断开时，会增删对应的绘图窗口和光标效果窗口。
- 这一策略符合直播时只共享其中一个屏幕的使用习惯。

主要代码：`AppDelegate.getCurrentScreen()`、`setupOverlayWindows()`、`screenParametersChanged()`。

### 3.2 快捷键策略

默认全局快捷键：

| 快捷键 | 功能 | 设计原因 |
| --- | --- | --- |
| `⌥Z` | 进入或退出画笔/标注状态 | 两个键、距离近、适合高频切换 |
| `⌥X` | 开启或关闭光圈与点击效果 | 与 `⌥Z` 相邻，且和绘图状态平级 |
| `Esc` | 标注状态下退出标注 | 绘图覆盖层接收并处理，不把事件继续交给下层网页 |
| `Space`、方向键、Page Up/Down | 标注状态下控制演示翻页 | 只在未编辑文字时发送给开启标注前的应用 |
| `⌥Delete` | 清空当前标注 | 减少对橡皮擦的依赖 |

工具单键只在标注层激活且没有修饰键时处理，因此正常浏览网页时不会抢占字母输入。默认包括：`P` 画笔、`A` 箭头、`L` 直线、`R` 圆角矩形，其他原版工具仍保留并可在设置中修改。

打开菜单栏菜单时会临时禁用三个全局快捷键并暂停自定义光标刷新，防止用户上下选择菜单项时误触或感觉卡顿。

### 3.3 绘图与演示指针是两个平级能力

这是本项目最重要的交互决策，后续不要重新做成“先进入画笔，再开启点击效果”的层级结构。

| 状态 | 网页能否点击 | 主要视觉 | 快捷键 |
| --- | --- | --- | --- |
| 正常状态 | 可以 | 系统鼠标；可选光圈和点击反馈 | `⌥X` 控制指针效果 |
| 标注状态 | 不操作下层网页，用鼠标绘图 | 自定义画笔/其他标注光标和笔画 | `⌥Z` 进入或退出 |
| 常驻穿透模式 | 可以 | 已有标注保留在页面上方 | 原版能力，非默认主路径 |

进入交互式标注状态时，光圈与点击效果会临时隐藏；退出标注后，如果之前是开启状态，会自动恢复。这样可以避免画笔光标、点击光圈和笔画同时出现造成视觉干扰。

“常驻穿透模式”仍保留，因为原版已有该能力；但它不是本定制版的核心路径。用户此前担心页面滚动或切换后，原有笔画与内容错位，这个判断仍然成立。除非明确需要固定画面上的长期标记，否则优先使用普通标注状态或独立的光圈/点击效果。

### 3.4 淡出和保留都需要

没有只保留一种方式：

- 淡出适合直播时快速圈重点，避免屏幕越画越乱。
- 保留适合讲解某张固定页面，直到手动清空或退出。
- 淡出时长可在 1–8 秒之间设置，默认 1.25 秒，步进 0.25 秒。
- 笔、荧光笔、箭头、直线、矩形和圆形都从鼠标松开后统一开始计时，解决长笔画“刚画完就消失”的问题。
- 当前透明度策略是前半段保持完全可见，后半段逐渐淡出。
- 文字仍按原版逻辑持续显示，不参与淡出。

### 3.5 画笔光标设计

标注状态必须有明显的视觉反馈，否则用户容易误以为仍能点击网页。

当前实现：

- 新增 `brush` 光标样式，并作为没有历史设置时的默认样式。
- 默认大小为 36，可在 20–56 之间调整，以适应 4K 显示器缩放到 1920×1080 的场景。
- 笔身使用固定石墨色 `#1F2937`，不跟随标注颜色，避免整支笔和线条混在一起。
- 最前端落笔点跟随当前标注颜色，例如红色线对应红色笔尖。
- 光标路径原点就是实际落笔点，笔尖与绘制坐标对齐。
- 当前代码包含白色外轮廓、笔尖附近的白色结构和轻微阴影，用于复杂背景下提高可见度。

设计讨论中曾对“整支笔是否需要白色外轮廓”有过保留意见。后续如继续调整，应同时在浅色网页、深色界面、复杂图片和直播压缩画面上测试，不能只看设置页预览。相对稳定的意图是：石墨色笔身、笔尖附近的白色区分、动态颜色落笔点；外轮廓粗细仍可以继续评估。

### 3.6 演示翻页键

直播时可能长时间保持标注层开启，同时用键盘控制 PowerPoint、Keynote、WPS、PDF 或浏览器中的 HTML 幻灯片。标注层不能独占空格和方向键。

确定的事件优先级是：

1. 正在编辑 Annotate 文字时，所有键盘事件优先交给文字输入框，包括空格、方向键、组合选择、按词移动和中文输入法候选操作。
2. 没有编辑文字时，先处理 Annotate 自己的 `Esc`、工具键、删除、撤销和重做。
3. 仍未处理的翻页白名单才发送给开启标注前的前台应用。

翻页白名单：

- 下一页：空格、右、下、Page Down。
- 上一页：左、上、Page Up。
- `Esc` 永远不发送给下层应用，避免退出 PPT 或网页全屏。
- 不发送字母键、Return、Home、End 和带 Command/Option/Control/Shift 的组合键。
- 长按产生的重复事件不连续发送，防止一次跳过多页。

目标不是识别“是不是 PowerPoint”，而是记录按 `⌥Z` 前正在使用的非 Annotate 应用。因此桌面演示软件和 Chrome、Safari、Edge 中的 HTML 幻灯片使用同一套逻辑。网页演示必须先点击幻灯片画面使其获得焦点，再开启标注；Annotate 不尝试读取浏览器标签页或 DOM 焦点。

此能力需要一个可持久化的“演示翻页键”开关。由于跨进程发送键盘事件受到 macOS 隐私保护，还必须检测 Post Event 权限、提供授权入口和失败反馈，不能静默失效。原来由空格触发的“淡出/保留切换”取消快捷键，只保留菜单和设置入口。

## 4. 已完成的修改

### 第一阶段：中文界面与授课绘图优化（`f054026`）

- 新增简体中文资源 `Annotate/zh-Hans.lproj/Localizable.strings`。
- 新增 `L10n` 封装，使 AppKit 和 SwiftUI 中的动态文本可以正确本地化。
- 调整设置页、菜单栏、工具名称、反馈提示和关于页中文显示。
- 矩形改为 12 pt 圆角矩形。
- 圆角路径同时用于绘制、选中命中测试和橡皮擦命中测试，避免视觉与交互边界不一致。
- 新增画笔光标基础样式与设置页预览。
- 使用独立 Bundle ID，避免与原版应用身份混用。

### 第二阶段：淡出时间与画笔可见性（`92f90ca`）

- 淡出时长从固定值改为 1–8 秒可配置。
- 笔画和形状统一在鼠标松开后开始淡出。
- 画笔尺寸默认值提高到 36，最大值提高到 56。
- 增加石墨色笔身、白色细节、外阴影和落笔点对齐。
- 菜单展开时暂停光标窗口动画和鼠标追踪，改善菜单操作卡顿。

### 第三阶段：动态颜色笔尖（`55847cd`）

- 画笔主体保持固定颜色。
- 只有最前端笔尖跟随当前标注颜色。
- 运行时绘制和设置页预览使用同一套视觉规则。

### 第四阶段：光圈/点击效果提升为全局能力（`466a151`）

- 新增独立全局快捷键 `⌥X`。
- 光圈和点击效果不再依附于画笔状态，可在正常操作网页时使用。
- 标注状态临时抑制指针效果，退出后恢复原设置。
- 设置页和菜单栏同步显示开关状态。
- 删除原先把点击效果当作标注层内工具快捷键的逻辑。

### 第五阶段：项目归属和发布整理（`4a2ae01`）

- 仓库命名为 `annotate-cn-custom`，本地应用继续叫 Annotate。
- README、About 和 `NOTICE.md` 明确感谢原作者并说明是非官方二次开发。
- 维护者同时写“AI 产品经理四月”和 GitHub 用户名 `@siryeah`。
- 移除原作者 Sparkle 更新源和公钥，关闭自动更新，避免定制版被原版覆盖。
- 发布 Apple Silicon 的 ZIP 和 DMG：`v1.4.1-cn.1`。

### 第六阶段：演示翻页键（2026-08-03，已发布 `v1.4.1-cn.2`）

- 开启交互式标注前记录前台应用，不区分桌面演示软件和浏览器 HTML 幻灯片。
- 标注时允许空格、方向键和 Page Up/Down 控制记录的演示应用。
- 文字编辑优先级最高，输入空格、移动光标、选择文字和输入法操作不触发翻页。
- `Esc`、工具键、删除、撤销和重做继续由 Annotate 处理；组合键和长按重复事件不发送给演示应用。
- 新增默认开启且可持久化的“演示翻页”设置、菜单开关、macOS 按键控制权限入口和失败提示。
- 修复用户拒绝过权限后再次点击“授权”没有可见反应的问题：请求仍未通过时，直接打开“系统设置 → 隐私与安全性 → 辅助功能”，返回应用后自动刷新状态。
- 移除空格切换“淡出/保留”的快捷键；该功能仍保留在菜单和“工具”设置中。

正式发布包使用应用版本 `1.4.1`、构建号 `4`，仅包含 `arm64`：

- `Annotate-CN-1.4.1-cn.2-arm64.zip`，SHA-256：`71bdc661c7cc73c56d9aaa2b2eae51d9504b6c7fc084082aaecdc3c80e7503e5`
- `Annotate-CN-1.4.1-cn.2-arm64.dmg`，SHA-256：`bfa14978095b735335634de2928cfe0539e8fc0aa14f1dd392095b7333db334f`

## 5. 当前没有做或明确暂缓的功能

- **内置截图：** 已讨论但明确暂缓。当前可使用 macOS 系统截图；不要把截图误写成已实现功能。
- **自动 Apple 公证：** 当前安装包只有 ad-hoc 签名，未经 Apple notarization。
- **Intel 通用包：** 当前 Release 是 arm64，仅面向 Apple Silicon。
- **动态改变整支画笔颜色：** 明确不做，只有笔尖跟随标注颜色。
- **向原作者提交 PR：** 当前没有提交，先维护自己的定制仓库。未来是否回馈上游需要另行评估。
- **大幅删减原版工具：** 没有执行。虽然最初 MVP 想精简，但后来决定保留原版大部分功能。

原版的计数器工具会按点击顺序生成 1、2、3 等编号标记，适合分步骤讲解；当前保留，没有为它增加截图能力。

## 6. 代码结构与修改入口

| 文件 | 作用 | 后续常见修改 |
| --- | --- | --- |
| `Annotate/AppDelegate.swift` | 菜单栏、全局快捷键、多屏窗口、状态切换、鼠标监听、演示目标与翻页事件发送 | 调整 `⌥Z`/`⌥X` 状态关系、多屏逻辑、演示翻页、菜单性能 |
| `Annotate/OverlayWindow.swift` | 接收鼠标和键盘事件、完成一次绘制 | 修改 `Esc`、演示翻页白名单、工具单键、鼠标松开计时 |
| `Annotate/OverlayView.swift` | 标注数据、绘制、淡出、选择、命中测试 | 圆角矩形、淡出算法、绘图行为 |
| `Annotate/CursorHighlightManager.swift` | 光圈、点击效果、自定义光标配置和状态 | 指针效果抑制、默认光标、颜色和尺寸 |
| `Annotate/CursorHighlightWindow.swift` | 每块屏幕上的透明指针效果窗口 | 录屏可见性、窗口层级和穿透行为 |
| `Annotate/CursorHighlightView.swift` | Core Animation 绘制光圈、点击反馈和画笔光标 | 画笔形状、白边、阴影、动态笔尖 |
| `Annotate/CursorSettingsView.swift` | 光标设置与实时预览 | 光标样式选择、大小和效果颜色 |
| `Annotate/ToolsSettingsView.swift` | 淡出/保留及时间设置 | 淡出范围、默认值和说明 |
| `Annotate/Constants.swift` | 全局快捷键、UserDefaults key、范围和默认值 | 快捷键和参数调整 |
| `Annotate/Localization.swift` | 本地化访问封装 | 动态字符串格式化 |
| `Annotate/zh-Hans.lproj/Localizable.strings` | 简体中文翻译 | 新增 UI 文案时必须补充 |
| `Annotate/AboutView.swift` | 版本、仓库链接和署名 | 仓库或维护者信息变更 |
| `project.yml` | XcodeGen 项目配置、版本号、Bundle ID | 新增文件、依赖、版本时修改 |
| `Package.swift` | Swift Package 配置和本地化资源 | SwiftPM 构建与测试 |
| `AnnotateTests/` | 单元测试 | 每次状态逻辑变化应先更新测试 |

关键持久化配置都在 `UserDefaults` 中。新增设置时，优先把 key 和默认范围集中放在 `Constants.swift`，不要把字符串 key 散落到多个视图。

## 7. 构建、测试与发布

### 7.1 开发环境

- macOS 14 或更高版本。
- Xcode。
- XcodeGen；`project.yml` 是项目结构的主要配置源。
- 依赖：`KeyboardShortcuts`、`Sparkle`。

`.gitignore` 忽略了 `Annotate.xcodeproj/project.pbxproj`。因此在新电脑或干净 clone 中，修改 `project.yml` 后必须重新生成工程：

```sh
xcodegen generate
```

发布时本机尚未安装 `xcodegen`，使用的是已经存在的本地生成工程。后续接手时建议先安装并验证 XcodeGen，避免 `project.yml` 与本地工程不一致。

### 7.2 测试命令

```sh
xcodebuild \
  -project Annotate.xcodeproj \
  -scheme Annotate \
  -configuration Debug \
  -derivedDataPath /tmp/annotate-derived-data \
  -skipPackageUpdates \
  -disableAutomaticPackageResolution \
  -onlyUsePackageVersionsFromResolvedFile \
  test \
  CODE_SIGNING_ALLOWED=NO
```

`v1.4.1-cn.1` 发布前结果：386 项测试执行，1 项因状态栏弹窗在测试环境不可见而跳过，0 项失败。

`v1.4.1-cn.2` 正式发布版结果：397 项测试，396 项通过，1 项因状态栏弹窗在测试环境不可见而跳过，0 项失败。ZIP 解压测试、解压后深度签名校验、DMG 校验和、只读挂载后的应用签名、版本号、架构和 `/Applications` 快捷入口均已验证，并已由维护者人工确认授权和演示翻页可用。

### 7.3 Release 构建

```sh
xcodebuild \
  -project Annotate.xcodeproj \
  -scheme Annotate \
  -configuration Release \
  -derivedDataPath /tmp/annotate-release-derived-data \
  -skipPackageUpdates \
  -disableAutomaticPackageResolution \
  -onlyUsePackageVersionsFromResolvedFile \
  build \
  CODE_SIGNING_ALLOWED=NO
```

发布前至少验证：

1. `codesign --verify --deep --strict Annotate.app`
2. 主程序为 `arm64`。
3. `CFBundleShortVersionString` 与 `CFBundleVersion` 正确。
4. `unzip -t` 通过。
5. `hdiutil verify` 通过，并实际只读挂载检查 `Annotate.app` 与 `/Applications` 快捷入口。
6. 计算 SHA-256，并与 GitHub Release 资产摘要对比。

本地没有 Developer ID 时，当前发布包使用 ad-hoc 签名且不启用 Hardened Runtime。Sparkle 内嵌组件必须从内到外逐个重新签名，最后再签主应用；否则主程序和内嵌组件的身份不一致，macOS 会显示“因为出现问题而无法打开”。正式对外分发时应改用 Developer ID、Hardened Runtime 和 Apple 公证。

仓库原有 Release Actions 只适用于具备 Developer ID 和公证密钥的标准版本。工作流会跳过标签名包含 `-cn.` 的中文定制版；中文定制版必须上传本地已经完成完整测试、签名验证和人工验证的原始 ZIP/DMG，不能由 CI 重新构建，否则 ad-hoc 签名身份会变化。

`dist/` 被 Git 忽略，安装包不会进入源码历史。长期备份依赖 GitHub Releases，不要只保留本地文件。

## 8. Git 与上游关系

```text
origin   https://github.com/siryeah/annotate-cn-custom.git
upstream https://github.com/epilande/Annotate.git
```

- `origin` 是自己的定制仓库，可以正常提交和发布。
- `upstream` 只用于获取原作者更新，未经明确决定不要直接推送，也不要自动创建 PR。
- 合并上游更新时应新建分支，先比较差异，再逐项回归多屏、快捷键、淡出、画笔光标和本地化测试。
- 原项目仍在发展，定制版版本号目前没有与上游版本建立自动对应关系。下一次发布前应先确定版本规则，例如继续使用 `v1.4.1-cn.2`，或升级为与新上游基线一致的版本。

必须保留 `LICENSE` 和 `NOTICE.md` 中的原作者信息。README 和 About 中也应同时提供“定制版仓库”和“原始项目”入口。

### 8.1 测试版与正式版必须隔离

后续给维护者测试的新构建，不得再直接覆盖正式版。至少同时区分以下三项：

- 正式版：应用名 `Annotate`，Bundle ID `com.siryeah.Annotate`，安装包名称不带 `beta`。
- 测试版：应用名 `Annotate 测试版`（英文可用 `Annotate Beta`），Bundle ID `com.siryeah.Annotate.beta`，安装包名称必须带 `beta` 和构建号。
- 两个版本使用各自的权限记录和配置数据；测试版验证通过后再提高正式版构建号并单独生成正式包。

只修改 Finder 中显示的应用名称是不够的，Bundle ID 也必须不同。测试版和正式版同时运行时会争用全局快捷键，因此默认只运行其中一个。ad-hoc 签名的每次重建仍可能改变 CDHash，影响该渠道自己的 TCC 权限；长期稳定方案仍是使用固定的 Developer ID 签名和 Apple 公证。

## 9. 录屏与直播兼容性说明

当前覆盖层使用透明高层级 `NSPanel`。从实现原理看，共享“整个显示器”比只捕捉浏览器单个窗口更有机会同时录到笔画、自定义画笔和光圈效果，但不同软件的捕获方式并不完全一致，不能宣称所有第三方软件均已兼容。

后续发布前建议建立固定测试矩阵：

| 场景 | 要验证的内容 |
| --- | --- |
| macOS 系统录屏 | 笔画、动态笔尖、光圈、点击效果是否全部出现 |
| OBS 显示器捕获 | 透明覆盖层和自定义光标是否被录到 |
| OBS 窗口捕获 | 是否漏掉独立覆盖层；如漏掉应提示使用显示器捕获 |
| 飞书/腾讯会议/Zoom 全屏共享 | 共享屏幕中笔画是否可见，帧率是否稳定 |
| 浏览器全屏播放 | `Esc` 退出标注时是否错误退出网页全屏 |
| 双屏共享单屏 | 只在鼠标所在且正在共享的屏幕出现标注 |

## 10. 已知问题与优先改进建议

1. **清理英文 README：** 下半部分仍残留原版“自动更新已启用”和旧光标尺寸说明，与定制版现状不完全一致。
2. **彻底整理 Sparkle：** 自动启动、Feed URL 和公钥已经移除，但依赖、控制器和部分检查更新代码仍保留。应决定是完整删除，还是为自己的仓库建立安全更新源。
3. **确定签名与公证方案：** 如果要给更多用户安装，建议使用 Apple Developer ID 签名和 notarization，减少 Gatekeeper 拦截。
4. **补齐 Intel 或 Universal 构建：** 只有确实存在 Intel 用户需求时再做，当前不要提前增加发布复杂度。
5. **完成录屏兼容性矩阵：** 尤其区分“共享整个屏幕”和“只共享单个窗口”。
6. **继续观察菜单卡顿：** 当前通过菜单展开时暂停追踪缓解，需要在 4K、双屏和高刷新率环境回归。
7. **评估画笔外轮廓：** 用真实直播画面测试白色外轮廓是否过重，再决定减细、取消或保留。
8. **截图功能仍为低优先级：** 除非系统截图明显打断授课流程，否则不建议先做内置截图。

## 11. 后续 AI 接手清单

接手后先做以下检查，不要直接改代码：

1. 阅读本文件、`README.zh-Hans.md`、`NOTICE.md` 和最近 5 个定制提交。
2. 运行 `git status`，不要覆盖维护者尚未提交的本地修改。
3. 确认当前分支、`origin` 和 `upstream`，不要误把定制改动推到原作者仓库。
4. 通过代码确认现有行为，不要只根据旧聊天记录推断。
5. 修改交互前先写清楚影响的是正常状态、标注状态还是常驻穿透状态。
6. 快捷键变化必须同时检查冲突、设置页、菜单显示、持久化和测试。
7. 光标变化必须同时修改运行时 Core Animation 绘制和设置页 SwiftUI 预览。
8. 新增 UI 文案必须补充简体中文本地化。
9. 运行相关单元测试，再运行完整测试和 Release 构建。
10. 发布前更新版本号、README、本文件、Release Notes、ZIP/DMG 和 SHA-256。

## 12. 后续需求记录模板

建议每次重要改动都追加一条，避免只存在聊天记录中：

```markdown
### YYYY-MM-DD：需求名称

- 使用场景：
- 当前问题：
- 决策：
- 明确不做：
- 交互与快捷键影响：
- 多屏与录屏影响：
- 修改文件：
- 测试结果：
- 对应提交或 Release：
```
